# 노동자 권리 보호 AI 어시스턴트 — 테스트 실행 결과 (로컬 실행분)

| 항목 | 내용 |
|---|---|
| 참고 문서 | 노동자권리보호_AI어시스턴트_테스트계획및결과보고서_v0.1.pdf |
| 실행일 | 2026-07-08 |
| 실행 환경 | 로컬 개발 서버 (`manage.py runserver`), 로컬 랭커(`localhost:8001`), SQLite |
| 실행 방식 | Playwright 실제 브라우저 자동화 + 일부 백엔드 직접 호출(유닛 성격 검증) |
| 범위 | 문서의 TC 중 **로컬에서 재현 가능한 항목 전체**. AWS/RDS/S3/실제 도메인 HTTPS 등 실인프라가 필요한 항목은 "로컬 실행 불가"로 별도 표기 |

이 문서는 v0.1 PDF의 TC ID 체계를 그대로 따르되, Pass/Fail을 실제 로컬 실행 결과로 채운 별도 결과 문서입니다.

---

## 2.1 인증 및 권한 (FR-001~010)

| TC ID | 결과 | 비고 |
|---|---|---|
| TC-AUTH-01 | **Pass** | 가입 후 자동 로그인, 로그아웃 후 재로그인 모두 성공. 비밀번호는 Django `create_user`가 기본적으로 PBKDF2 해시로 저장 |
| TC-AUTH-02 | **Pass** | 비밀번호 불일치 시 "이메일 또는 비밀번호가 일치하지 않습니다" 노출 |
| TC-AUTH-03 | **Pass** | DB에서 세션의 `expire_date`를 과거로 직접 조작해 60분 경과를 재현 → 이후 요청 시 `/login/`으로 리다이렉트되어 세션 만료 확인 |
| TC-AUTH-04 | **Pass** | 5회 연속 실패 후, **올바른 비밀번호**로 시도해도 "로그인 시도가 너무 많습니다" 메시지로 계속 차단됨을 확인 |
| TC-AUTH-05 | **Pass** | 일반 가입 절차로는 `is_staff`가 부여되지 않아 관리자 콘솔 접근 시 403 |

## 2.2 QA 법률 질의응답 (FR-020~025)

| TC ID | 결과 | 비고 |
|---|---|---|
| TC-QA-01 | **Pass** | 자연어 질문 정상 답변 생성 (459자) |
| TC-QA-02 | **Pass** | 판례 검색 → 법령 검색 → 답변 생성 순으로 진행 이벤트 확인 |
| TC-QA-03 | **⚠️ Fail** | "판례/법령 조문은 필요없고 절차만 궁금하다"고 명시한 순수 절차 질문에도 실제로는 판례 검색·법령 검색이 생략되지 않고 실행됨. 라우팅이 기대만큼 `procedure_guidance` 단독으로 좁혀지지 않는 것으로 보임 |
| TC-QA-04 | **Pass** | 답변 근거 드로어에서 법령(조문 포함)·판례(사건번호 포함) 모두 정상 노출 |
| TC-QA-05 | 미실행 | 복합 질문 멀티에이전트 순차 실행은 이번 회차에서 실측하지 않음 (코드상 `MAX_ITERATIONS=3` 존재는 기존에 확인됨) |
| TC-QA-06 | **Pass** | 처리 중 스피너+실시간 노드 진행 라벨 노출 확인 |

## 2.3 권리찾기 (FR-030~032) / 2.4 증거자료 관리 (FR-050~051)

**스코프 제외** — 기능 자체가 구현되어 있지 않아 실행 대상에서 제외 (사전에 합의된 사항).

## 2.5 계산기 (FR-060~065)

| TC ID | 결과 | 비고 |
|---|---|---|
| TC-CALC-01 | **⚠️ 부분 Pass** | 퇴직금 계산 결과는 정상 표시되나, **법령 근거(조문 인용)는 표시되지 않음** — 산식·금액만 노출. 이번 실행에서 새로 발견된 항목 |
| TC-CALC-02 | **Pass** | 연차수당 계산 결과 정상 |
| TC-CALC-03 | **Pass** | 주휴수당 계산 및 최저임금 위반 여부(위반 의심 69.7%) 정상 판정 |
| TC-CALC-04 | **Pass** | 음수/빈 값 입력 시 실시간 오류 표시 + 제출 버튼 비활성화(계산 중단) 확인 |

## 2.6 최신 뉴스 (FR-090~092)

| TC ID | 결과 | 비고 |
|---|---|---|
| TC-NEWS-01 | **Pass** | 키워드 검색 → 기사 5건 반환 |
| TC-NEWS-02 | **Pass** | ReAct 요약 생성 확인 |
| TC-NEWS-03 | **Pass** | 네이버 API 인증정보를 실제로 깨뜨려 401을 유도 → 백업 캐시로 대체되고 "최신 내용이 아닐 수 있습니다" 안내 문구 노출 확인 |
| TC-NEWS-04 | **Pass** | 결과 없는 키워드 → 안내 메시지 표시 |

## 2.7 마이페이지 (FR-095~098)

| TC ID | 결과 | 비고 |
|---|---|---|
| TC-MYPAGE-01 | **Pass** | 상담이력↔계정정보 탭 전환 정상 |
| TC-MYPAGE-02 | **Pass** | 이력 카드 20개 노출, 클릭 시 상세 정상 확장 |
| TC-MYPAGE-03 | **Pass** | 이름/이메일/가입일/총 상담횟수 정상 표시 |
| TC-MYPAGE-04 | **⚠️ 문구 사소한 차이** | 신규 계정 조회 시 "상담 이력이 없습니다" 노출(기능은 정상). 문서 기대 문구는 "아직 상담 이력이 없습니다"로 "아직"이 빠져있음 |

## 2.8 관리자 — 대시보드 (FR-100~105)

| TC ID | 결과 | 비고 |
|---|---|---|
| TC-ADMIN-DASH-01 | **Pass** | 요약 카드 4개 정상 |
| TC-ADMIN-DASH-02 | **Pass** | 일별 추이 막대 정상 렌더링 |
| TC-ADMIN-DASH-03 | **Pass** | 카테고리 도넛차트/범례 정상 |
| TC-ADMIN-DASH-04 | **Pass** | 사용자 현황 → 사용자 관리 화면 이동 정상 |

## 2.9 관리자 — 사용자 관리 (FR-110~116)

| TC ID | 결과 | 비고 |
|---|---|---|
| TC-ADMIN-USER-01 | **Pass** | 전체/활성/정지 수 정상 표시 |
| TC-ADMIN-USER-02 | **Pass** | 이름 검색 필터 정상 동작 |
| TC-ADMIN-USER-03 | **Pass** | 계정 정지 클릭 시 상태 즉시 반영(원복까지 확인) |
| TC-ADMIN-USER-04 | **Pass** | CSV 내보내기 다운로드 정상 |
| TC-ADMIN-USER-05 | **Pass** | (이전 회차에서 수정 완료) 정지 계정+정확한 비밀번호 시 "정지된 계정입니다" 전용 안내 노출 |

## 2.10 관리자 — 피드백 관리 (FR-120~124)

| TC ID | 결과 | 비고 |
|---|---|---|
| TC-ADMIN-FEEDBACK-01 | **Pass** | 좋아요/싫어요/평균 만족도 요약 정상 |
| TC-ADMIN-FEEDBACK-02 | **Pass** | 카테고리별 만족도 오름차순 정렬 확인 |
| TC-ADMIN-FEEDBACK-03 | **Pass** | "개선 필요" 탭 필터링 및 아코디언 확장 모두 정상 |
| TC-ADMIN-FEEDBACK-04 | **Pass** | 만족도 낮은 카테고리에 "개선 필요" 배지 정상 노출 |

## 2.11 관리자 — 프롬프트 관리 (FR-130~135)

| TC ID | 결과 | 비고 |
|---|---|---|
| TC-ADMIN-PROMPT-01 | **Pass** | 5종 템플릿 모두 조회/편집 가능 |
| TC-ADMIN-PROMPT-02 | **Pass** | 저장 후 새 버전(v2)이 수정자·일시와 함께 이력에 자동 생성 |
| TC-ADMIN-PROMPT-03 | **Pass** | 이전 버전(v1)으로 롤백 시 내용 정상 복원 |
| TC-ADMIN-PROMPT-04 | **Pass** | (이전 회차에서 수정 완료) "validate" 없이 "save" 직접 호출해도 서버가 플레이스홀더 누락을 차단 |
| TC-ADMIN-PROMPT-05 | **Pass** | 저장 클릭 시 "즉시 적용" 확인창 노출 확인 |

## 2.12 관리자 — 성능 모니터링 (FR-150~153)

| TC ID | 결과 | 비고 |
|---|---|---|
| TC-ADMIN-PERF-01 | **Pass** | 노드별 평균/최대 처리시간 정상 표시 (다만 이전 테스트로 쌓인 `test` 노드 더미데이터가 함께 노출됨 — 데이터 정리 필요) |
| TC-ADMIN-PERF-02 | **Pass** | 총 호출/토큰/비용 표시, 일·주·월 단위 전환 정상 |
| TC-ADMIN-PERF-03 | **❌ Fail** | (기존에 확인된 사항) 병목 노드 요약 그래프만 있고, 실제 질의 내용을 조회하는 "목록" 기능은 없음 |
| TC-ADMIN-PERF-04 | **Pass** | 최근 30일 API 호출 그래프 정상 렌더링 |

## 3. LLM API 연동 테스트

| TC ID | 결과 | 비고 |
|---|---|---|
| TC-LLM-01 | **Pass** | SSE 기반 진행상황 스트리밍으로 논블로킹 진행상태 노출 확인 (폴링/웹소켓은 아니지만 동일한 목적의 비차단 스트리밍) |
| TC-LLM-02 | **Pass(설정 확인)** | `LLM_TIMEOUT_SECONDS=30`, `LLM_MAX_RETRIES=3` 설정 확인. openai SDK가 내부적으로 지수 백오프 재시도 수행(실제 30초 타임아웃 실측은 비용/시간상 생략) |
| TC-LLM-03 | **Pass** | 401/429/5xx 각각 다른 안내 메시지 반환 확인 (유닛 테스트) |
| TC-LLM-04 | **Pass** | (이전 회차에서 확대 완료) AI 상담·계산기·뉴스 응답 모두에 법률 자문 고지 문구 노출 |
| TC-LLM-05 (NFR-001) | **❌ Fail** | 실측: RAG 파이프라인 평균 15.3초, p95 18.5초 (n=2, 로컬 CPU 랭커 기준). "평균 10초 이내" 기준 미충족. RunPod 콜드스타트가 아니어도 실패하는 항목 |
| TC-EMB-01/02 | **간접 확인** | 별도 단위 테스트는 미실행. 실제 RAG 질의에서 판례/법령이 정상 검색·반환되는 것으로 임베딩 파이프라인 자체는 정상 동작 중임을 간접 확인 |
| TC-NEWSAPI-01 | **Pass** | 정상 키워드 검색 확인 |
| TC-NEWSAPI-02 | **코드 확인만** | `MAX_STEPS`/재검색 로직 존재 확인, 실제 3회 재호출 유도 시나리오는 미실행 |
| TC-NEWSAPI-03 | **Pass** | 실제 401 유도로 캐시 대체 확인 (TC-NEWS-03과 동일 근거) |
| TC-PERF-01 | **❌ Fail** | 실측: 판례 검색+리랭킹 평균 3.45초, 최대 6.49초 (n=2, **로컬 CPU 랭커 기준**). "3초 이내" 기준을 로컬에서도 초과 — RunPod 콜드스타트 문제와는 별개로, CPU 기반 CrossEncoder 리랭킹 자체가 원래 느림 |

## 4. 배포 환경 검증

| TC ID | 결과 | 비고 |
|---|---|---|
| TC-DEPLOY-01~03 | **로컬 실행 불가** | Docker 재빌드/재현성, 서버 재기동 시 데이터 영속성, 실제 CI/CD 파이프라인 실행은 로컬 환경만으로 의미 있게 재현할 수 없어 미실행 |

## 4.2 반응형 UI 및 보안

| TC ID | 결과 | 비고 |
|---|---|---|
| TC-UI-01~02 | **Pass** | 1920px(데스크톱)/360px/768px(모바일) 모두 가로 스크롤 없음, 모바일 전환 시 사이드바 접이식 UI 정상 노출 |
| TC-SEC (민감정보 커밋) | **Pass** | `.env`가 git에 커밋되지 않고 `.gitignore`로 확실히 제외됨을 확인 |
| TC-SEC (세션 쿠키) | **부분 Pass** | `HttpOnly=True`, `SameSite=Lax` 확인. `Secure=False` — HTTPS 미설정 상태라 당연한 결과이며 별도 이슈(HTTPS 미구현)와 동일한 근본 원인 |
| TC-SEC (HTTPS 전송) | **❌ Fail** | (기존에 확인된 사항) `nginx.conf`에 443/SSL 설정 없음. 로컬 개발 환경 특성상 애초에 검증 불가 |
| TC-SEC (타인 데이터 접근 시 403) | **설계상 차이** | (기존에 확인된 사항) `HistoryDetailView`는 IDOR 방지 관례에 따라 403 대신 404를 반환하도록 의도적으로 설계됨 |

---

## 종합 요약

| 구분 | Pass | Fail | 부분/보류 | 로컬 실행 불가 |
|---|---:|---:|---:|---:|
| 건수(대략) | 45 | 5 | 6 | 3 |

### 이번 실행에서 새로 발견된 항목
- **TC-QA-03**: 순수 절차 질문에도 판례/법령 검색이 생략되지 않음
- **TC-CALC-01**: 퇴직금 계산 결과에 법령 근거(조문) 미표시
- **TC-LLM-05 / NFR-001 / TC-PERF-01**: 로컬 CPU 랭커 기준으로도 "3초/평균 10초" 성능 기준 실측 미달 (기존엔 RunPod 콜드스타트만 원인으로 지목했으나, CPU 리랭킹 자체가 근본 원인임이 실측으로 확인됨)
- **TC-ADMIN-PERF-01**: 대시보드에 이전 테스트용 `test` 더미 노드 데이터가 섞여 노출됨 (데이터 정리 필요, 기능 결함은 아님)

### 기존에 이미 알려져 있던 미해결 항목 (변화 없음)
- TC-ADMIN-PERF-03(임계치 초과 질의 목록 조회 불가)
- TC-SEC(HTTPS 미구성)
- TC-DEPLOY(실인프라 필요, 로컬 검증 불가)
- 권리찾기(FR-030~032)·증거자료 체크리스트(FR-050~051)·S3 (스코프 제외 합의)
